{
  "name": "market-web-application",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --cwd backend dev\" \"bun run --cwd frontend dev\"",
    "dev:backend": "bun run --cwd backend dev",
    "dev:frontend": "bun run --cwd frontend dev",
    "seed": "bun run --cwd backend seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
