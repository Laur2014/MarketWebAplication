# Demo Assets Placeholder

Add one of the following before final repository submission:

- screenshots of dashboard, market detail, profile, leaderboard, admin resolve/archive flow
- or a public video link file (e.g. `video-link.txt`)

Current implementation is ready for capture after running:

```bash
bun install
bun run seed
bun run dev
```
